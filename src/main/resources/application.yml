spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fitzealot-diet?createDatabaseIfNotExist=true
    username: root
    password: 2007

  jpa:
    hibernate:
      ddl-auto: update   # Options: none, update, create, create-drop
    show-sql: true       # Shows SQL in console (for debugging)

  # Optional: Format SQL output
  jpa.properties.hibernate.format_sql: true

server:
  port: 8082
